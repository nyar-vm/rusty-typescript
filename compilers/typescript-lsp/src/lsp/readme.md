# TypeScript LSP Module

This module provides the Language Server Protocol (LSP) implementation for TypeScript.

## Features

- `lsp` - Enable LSP functionality
- `oak-highlight` - Enable syntax highlighting
- `oak-pretty-print` - Enable code formatting

## TypeScriptLanguageService

The main service that implements the LSP protocol for TypeScript.
