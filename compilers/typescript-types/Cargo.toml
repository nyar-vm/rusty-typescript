[package]
name = "typescript-types"
version = { workspace = true }
edition = { workspace = true }
authors = { workspace = true }
license = { workspace = true }
description = "Type definitions for TypeScript compiler"

[dependencies]
serde = { workspace = true, features = ["derive"] }
